---
import AppLayout from '@/layouts/AppLayout.astro';
import TasksApp from '@/components/tasks/TasksApp';
import { getSnapshot } from '@/features/tasks/ops/store';
import { ensureSyncLoop, getSyncStatus } from '@/features/tasks/ops/sync-loop';

ensureSyncLoop();
const snapshot = await getSnapshot();
const initial = { ...snapshot, sync: getSyncStatus() };
---

<AppLayout title="Tasks">
  <TasksApp client:load initial={initial} />
</AppLayout>
